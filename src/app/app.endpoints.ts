import { environment } from '../environments/environment';

export const GET_POSTS = environment.endTypicodePlaceholder + '/posts';
export const GET_USERS = environment.endTypicodePlaceholder + '/users';
export const GET_IMAGES = environment.endTypicodePlaceholder + '/photos';
